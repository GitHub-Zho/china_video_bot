# China Video Bot — all free APIs (Anthropic optional, not used by default)

# LLM (generation) + Vision (verification, via REST)
groq>=0.9.0                 # Director / Critic (free Llama 3.3 70B)
requests>=2.31.0           # Gemini vision (verifier) + Pexels/Pixabay/Unsplash

# TTS
kokoro-onnx>=0.4.0         # primary voice (local, free, natural)
soundfile>=0.12.0          # write Kokoro audio
edge-tts>=7.2.0            # fallback voice

# Media / video
numpy>=1.24.0
Pillow>=10.0.0
moviepy>=2.1.0             # legacy image-only path (FFmpeg is the main path)
# NOTE: ffmpeg + ffprobe must be installed and compiled WITH libfreetype
# (the drawtext filter). conda-forge ffmpeg has it; Homebrew ffmpeg 8.x may not.
# See data/AUTONOMOUS_GUIDE.md.

# YouTube publishing + analytics
google-api-python-client>=2.130.0
google-auth-oauthlib>=1.2.0

# Scheduling + config
apscheduler>=3.10.4
python-dotenv>=1.0.0

# Optional: style learning from a YouTube URL
# yt-dlp>=2024.0.0
